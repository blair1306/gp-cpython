<dec f='include/lzma/filter.h' l='178' type='lzma_ret lzma_raw_encoder(lzma_stream * strm, const lzma_filter * filters)'/>
<use f='codebrowser/Modules/_lzmamodule.c' l='683' u='c' c='Compressor_init_raw'/>
<doc f='include/lzma/filter.h' l='161'>/**
 * \brief       Initialize raw encoder
 *
 * This function may be useful when implementing custom file formats.
 *
 * \param       strm    Pointer to properly prepared lzma_stream
 * \param       filters Array of lzma_filter structures. The end of the
 *                      array must be marked with .id = LZMA_VLI_UNKNOWN.
 *
 * The `action&apos; with lzma_code() can be LZMA_RUN, LZMA_SYNC_FLUSH (if the
 * filter chain supports it), or LZMA_FINISH.
 *
 * \return      - LZMA_OK
 *              - LZMA_MEM_ERROR
 *              - LZMA_OPTIONS_ERROR
 *              - LZMA_PROG_ERROR
 */</doc>
